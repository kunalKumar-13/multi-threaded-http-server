This is a slightly larger sample text file intended for testing purposes. It ensures that the server can handle file transfers that might require more than a single buffer. The purpose is to verify data integrity over a multi-chunk transfer.
(Add several more paragraphs of text here to make the file larger)